{
  "name": "next5",
  "version": "1.0.0",
  "scripts": {
    "dev": "NODE_OPTIONS='--inspect' next dev",
    "build": "next build",
    "prebuild": "npm run ci && npm uninstall postcss-d-ts && npm install ../..",
    "ci": "npm ci --prefer-offline --no-audit --silent --quiet --no-progress"
  },
  "license": "MIT",
  "dependencies": {
    "next": "^10.0.6",
    "postcss": "^8.2.8",
    "postcss-d-ts": "file:../.."
  }
}
