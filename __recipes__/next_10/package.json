{
  "name": "next5",
  "version": "1.0.0",
  "scripts": {
    "dev": "NODE_OPTIONS='--inspect' next dev",
    "build": "next build && next export",
    "prebuild": "npm uninstall postcss-plugin-d-ts && npm install ../.."
  },
  "license": "MIT",
  "dependencies": {
    "next": "^10.0.6",
    "postcss": "^8.2.8",
    "postcss-plugin-d-ts": "file:../.."
  }
}
